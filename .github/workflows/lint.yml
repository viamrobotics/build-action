on:
  push:
  
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: install
      run: npm install
    - name: lint
      run: eslint src
    - name: check ncc
      run: |
        make dist/index.js
        git status
